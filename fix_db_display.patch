--- db_display.cpp.orig
+++ db_display.cpp
@@ -1,4 +1,5 @@
 #include "db_display.h"
+#include "blink_manager.h"
 #include <iostream>
 #include <cstring>
 
@@ -23,9 +24,9 @@
     }
     
     // Handle border blinking
-    bool shouldBlink = shouldBlink(dbValue);
-    bool borderShouldBeVisible = !shouldBlink || BlinkManager().getCurrentState();
-    bool borderWasVisible = !shouldBlink || lastBlinkState_;
+    bool shouldBlinkValue = shouldBlink(dbValue);
+    bool borderShouldBeVisible = !shouldBlinkValue || BlinkManager().getCurrentState();
+    bool borderWasVisible = !shouldBlinkValue || lastBlinkState_;
     
     if(borderShouldBeVisible != borderWasVisible || dbValue != lastDbValue_) {
         if(borderShouldBeVisible) {
@@ -48,7 +49,6 @@
 }
 
 void DbDisplay::drawText(int dbValue, int componentStartY) {
-    int cols = offscreen_->width();
     
     // Load fonts
     rgb_matrix::Font largeFont, smallFont;
-    if (!largeFont.LoadFont("../fonts/7x13.bdf")) {
+    if (!largeFont.LoadFont("../../fonts/7x13.bdf")) {
         std::cerr << "Large font not found, skipping text\n";
         return;
     }
-    if (!smallFont.LoadFont("../fonts/5x7.bdf")) {
+    if (!smallFont.LoadFont("../../fonts/5x7.bdf")) {
         std::cerr << "Small font not found, using large font\n";
         smallFont = largeFont;
     }
